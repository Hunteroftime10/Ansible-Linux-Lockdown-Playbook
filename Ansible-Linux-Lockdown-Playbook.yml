---
  - hosts: all
    become: true
    tasks:
    - name: Install Tree to client
      apt:
        name: tree
        state: latest

    - name: Checking all user files for unwanted files
      ansible.builtin.shell:
        cmd: tree -f /home |grep .png > file_list
